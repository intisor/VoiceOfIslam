# Bug Documentation Timeline (bugs_2026-04-29_12.md)

Append-only bug audit trail generated by CodeNarrator.

<!-- bug-entry:diagnostic:eOfIslam_Programcs_L32_iservicecollection-does-not-contain_mok0rh7v -->
    ## Bug Report: `eOfIslam_Programcs_L32_iservicecollection-does-not-contain_mok0rh7v`

| Field | Value |
|---|---|
| **File** | `VoiceOfIslam/Program.cs` |
| **Line** | 32 (col 18) |
| **Source** | unknown |
| **Severity** | 🔴 Error |
| **Detected** | 2026-04-29 12:16:31 UTC |
| **Resolved** | 2026-04-29 12:19:31 UTC |
| **Time to Fix** | 3m 0s |
| **Status** | ✅ Resolved |

---

## Error Message

```
'IServiceCollection' does not contain a definition for 'AddDbContextFactory' and no accessible extension method 'AddDbContextFactory' accepting a first argument of type 'IServiceCollection' could be found (are you missing a using directive or an assembly reference?)
```

---

## Code Context

```
  29: var connectionString = builder.Configuration.GetConnectionString("DefaultConnection");
  30: 
  31: // Using AddDbContextFactory for Blazor performance and thread-safety
  32: builder.Services.AddDbContextFactory<AppDbContext>(options => ◄
  33: 	options.UseNpgsql(connectionString));
  34: 
  35: var app = builder.Build();
```

---

## Predicted Fix *(CodeNarrator — before any fix was applied)*

> _LLM analysis failed: Error: Error on conversation request. Check the log for more details._

---

## Actual Fix Applied

```diff
diff --git a/VoiceOfIslam.Api/Program.cs b/VoiceOfIslam.Api/Program.cs
index 859c89b..6eb8d24 100644
--- a/VoiceOfIslam.Api/Program.cs
+++ b/VoiceOfIslam.Api/Program.cs
@@ -1,12 +1,12 @@
 using Microsoft.AspNetCore.Builder;
 using Microsoft.Extensions.DependencyInjection;
 using Microsoft.Extensions.Hosting;
+using VoiceOfIslam.Api.Services;
 using VoiceOfIslam.Shared.Models;
 
 var builder = WebApplication.CreateBuilder(args);
 
-// Add services to the container if needed
-// builder.Services.AddCors();
+builder.Services.AddSingleton<AudioService>();
 
 var app = builder.Build();
 
@@ -14,49 +14,17 @@ var app = builder.Build();
 app.UseDefaultFiles();
 app.UseStaticFiles();
 
-app.UseRouting(); 
-// API endpoints
+app.UseRouting();
 
-// In-memory sample data for demonstration
-var audioStreams = new List<AudioStream>
+app.MapGet("/api/audio-streams", (AudioService audioService) => audioService.GetPastAudios());
+app.MapGet("/api/audio-streams/live", (AudioService audioService) => audioService.GetLiveLecture());
+app.MapGet("/api/audio-streams/recent/{count:int}", (int count, AudioService audioService) => audioService.GetRecentLectures(count));
+app.MapGet("/api/audio-streams/{id:guid}/playback-url", async (Guid id, AudioService audioService) =>
 {
-    new AudioStream {
-        Id = Guid.NewGuid(),
-        Title = "Sample Lecture 1",
-        Description = "Introduction to Islam",
-        BlobUrl = "https://example.com/audio1.mp3",
-        CreatedAt = DateTime.UtcNow.AddDays(-2),
-        Speaker = "Imam Ali",
-        Duration = TimeSpan.FromMinutes(45)
-    },
-    new AudioStream {
-        Id = Guid.NewGuid(),
-        Title = "Sample Lecture 2",
-        Description = "History of Prophets",
-        BlobUrl = "https://example.com/audio2.mp3",
-        CreatedAt = DateTime.UtcNow.AddDays(-1),
-        Speaker = "Imam Bilal",
-        Duration = TimeSpan.FromMinutes(50)
-    }
-};
-
-// Endpoint: List all audio streams (blobs)
-app.MapGet("/api/blobs", () => audioStreams);
-
-// Endpoint: Download a single audio stream (blob) by ID
-app.MapGet("/api/blobs/{id}", (Guid id) =>
-{
-    var stream = audioStreams.FirstOrDefault(a => a.Id == id);
-    return stream is not null ? Results.Ok(stream) : Results.NotFound();
-});
-
-// Endpoint: Upload a new audio stream (blob) (optional, demo only)
-app.MapPost("/api/blobs", (AudioStream newStream) =>
-{
-    newStream.Id = Guid.NewGuid();
-    newStream.CreatedAt = DateTime.UtcNow;
-    audioStreams.Add(newStream);
-    return Results.Created($"/api/blobs/{newStream.Id}", newStream);
+    var playbackUrl = await audioService.GetAuthorizedPlaybackUrl(id);
+    return string.IsNullOrWhiteSpace(playbackUrl)
+        ? Results.NotFound()
+        : Results.Ok(new PlaybackUrlResponse { Url = playbackUrl });
 });
 
 app.MapFallbackToFile("index.html");

```

---

## Validation

> _Validation LLM call failed: Error: Error on conversation request. Check the log for more details._

---

*Generated by [CodeNarrator](https://github.com/codenarrator/codebase-narrator) · 2026-04-29 12:16:31 UTC*

<!-- bug-entry:diagnostic:IslamApi_Programcs_L13_the-type-or-namespace_mok11s40 -->
    ## Bug Report: `IslamApi_Programcs_L13_the-type-or-namespace_mok11s40`

| Field | Value |
|---|---|
| **File** | `VoiceOfIslam.Api/Program.cs` |
| **Line** | 13 (col 38) |
| **Source** | unknown |
| **Severity** | 🔴 Error |
| **Detected** | 2026-04-29 12:24:32 UTC |
| **Resolved** | 2026-04-29 12:25:35 UTC |
| **Time to Fix** | 1m 3s |
| **Status** | ✅ Resolved |

---

## Error Message

```
The type or namespace name 'AppDbContext' could not be found (are you missing a using directive or an assembly reference?)
```

---

## Code Context

```
  10: 
  11: var connectionString = builder.Configuration.GetConnectionString("DefaultConnection");
  12: 
  13: builder.Services.AddDbContextFactory<AppDbContext>(options => ◄
  14:     options.UseNpgsql(connectionString));
  15: 
  16: builder.Services.AddScoped<BlobSasService>();
```

---

## Predicted Fix *(CodeNarrator — before any fix was applied)*

> _LLM analysis failed: ChatQuotaExceeded: You've exhausted your premium model quota. Please enable additional paid premium requests, upgrade to Copilot Pro+, or wait for your allowance to renew._

---

## Actual Fix Applied

```diff
diff --git a/VoiceOfIslam.Api/Program.cs b/VoiceOfIslam.Api/Program.cs
index 859c89b..662acb3 100644
--- a/VoiceOfIslam.Api/Program.cs
+++ b/VoiceOfIslam.Api/Program.cs
@@ -1,12 +1,23 @@
 using Microsoft.AspNetCore.Builder;
+using Microsoft.EntityFrameworkCore;
 using Microsoft.Extensions.DependencyInjection;
 using Microsoft.Extensions.Hosting;
+using VoiceOfIslam.Api.Data;
+using VoiceOfIslam.Api.Services;
 using VoiceOfIslam.Shared.Models;
 
 var builder = WebApplication.CreateBuilder(args);
 
-// Add services to the container if needed
-// builder.Services.AddCors();
+var connectionString = builder.Configuration.GetConnectionString("DefaultConnection");
+
+builder.Services.AddDbContextFactory<AppDbContext>(options =>
+    options.UseNpgsql(connectionString));
+
+builder.Services.AddScoped<BlobSasService>();
+builder.Services.AddScoped<AudioService>();
+
+builder.Services.AddOptions<BlobStorageOptions>()
+    .BindConfiguration(BlobStorageOptions.SectionName);
 
 var app = builder.Build();
 
@@ -14,49 +25,17 @@ var app = builder.Build();
 app.UseDefaultFiles();
 app.UseStaticFiles();
 
-app.UseRouting(); 
-// API endpoints
-
-// In-memory sample data for demonstration
-var audioStreams = new List<AudioStream>
-{
-    new AudioStream {
-        Id = Guid.NewGuid(),
-        Title = "Sample Lecture 1",
-        Description = "Introduction to Islam",
-        BlobUrl = "https://example.com/audio1.mp3",
-        CreatedAt = DateTime.UtcNow.AddDays(-2),
-        Speaker = "Imam Ali",
-        Duration = TimeSpan.FromMinutes(45)
-    },
-    new AudioStream {
-        Id = Guid.NewGuid(),
-        Title = "Sample Lecture 2",
-        Description = "History of Prophets",
-        BlobUrl = "https://example.com/audio2.mp3",
-        CreatedAt = DateTime.UtcNow.AddDays(-1),
-        Speaker = "Imam Bilal",
-        Duration = TimeSpan.FromMinutes(50)
-    }
-};
-
-// Endpoint: List all audio streams (blobs)
-app.MapGet("/api/blobs", () => audioStreams);
-
-// Endpoint: Download a single audio stream (blob) by ID
-app.MapGet("/api/blobs/{id}", (Guid id) =>
-{
-    var stream = audioStreams.FirstOrDefault(a => a.Id == id);
-    return stream is not null ? Results.Ok(stream) : Results.NotFound();
-});
+app.UseRouting();
 
-// Endpoint: Upload a new audio stream (blob) (optional, demo only)
-app.MapPost("/api/blobs", (AudioStream newStream) =>
+app.MapGet("/api/audio-streams", async (AudioService audioService) => await audioService.GetPastAudios());
+app.MapGet("/api/audio-streams/live", async (AudioService audioService) => await audioService.GetLiveLecture());
+app.MapGet("/api/audio-streams/recent/{count:int}", async (int count, AudioService audioService) => await audioService.GetRecentLectures(count));
+app.MapGet("/api/audio-streams/{id:guid}/playback-url", async (Guid id, AudioService audioService) =>
 {
-    newStream.Id = Guid.NewGuid();
-    newStream.CreatedAt = DateTime.UtcNow;
-    audioStreams.Add(newStream);
-    return Results.Created($"/api/blobs/{newStream.Id}", newStream);
+    var playbackUrl = await audioService.GetAuthorizedPlaybackUrl(id);
+    return string.IsNullOrWhiteSpace(playbackUrl)
+        ? Results.NotFound()
+        : Results.Ok(new PlaybackUrlResponse { Url = playbackUrl });
 });
 
 app.MapFallbackToFile("index.html");

```

---

## Validation

> _Validation LLM call failed: ChatQuotaExceeded: You've exhausted your premium model quota. Please enable additional paid premium requests, upgrade to Copilot Pro+, or wait for your allowance to renew._

---

*Generated by [CodeNarrator](https://github.com/codenarrator/codebase-narrator) · 2026-04-29 12:24:32 UTC*

<!-- bug-entry:diagnostic:IslamApi_Programcs_L16_the-type-or-namespace_mok11s40 -->
    ## Bug Report: `IslamApi_Programcs_L16_the-type-or-namespace_mok11s40`

| Field | Value |
|---|---|
| **File** | `VoiceOfIslam.Api/Program.cs` |
| **Line** | 16 (col 28) |
| **Source** | unknown |
| **Severity** | 🔴 Error |
| **Detected** | 2026-04-29 12:24:32 UTC |
| **Resolved** | 2026-04-29 12:25:53 UTC |
| **Time to Fix** | 1m 21s |
| **Status** | ✅ Resolved |

---

## Error Message

```
The type or namespace name 'BlobSasService' could not be found (are you missing a using directive or an assembly reference?)
```

---

## Code Context

```
  13: builder.Services.AddDbContextFactory<AppDbContext>(options =>
  14:     options.UseNpgsql(connectionString));
  15: 
  16: builder.Services.AddScoped<BlobSasService>(); ◄
  17: builder.Services.AddScoped<AudioService>();
  18: 
  19: builder.Services.AddOptions<BlobStorageOptions>()
```

---

## Predicted Fix *(CodeNarrator — before any fix was applied)*

> _LLM analysis failed: ChatQuotaExceeded: You've exhausted your premium model quota. Please enable additional paid premium requests, upgrade to Copilot Pro+, or wait for your allowance to renew._

---

## Actual Fix Applied

```diff
diff --git a/VoiceOfIslam.Api/Program.cs b/VoiceOfIslam.Api/Program.cs
index 859c89b..662acb3 100644
--- a/VoiceOfIslam.Api/Program.cs
+++ b/VoiceOfIslam.Api/Program.cs
@@ -1,12 +1,23 @@
 using Microsoft.AspNetCore.Builder;
+using Microsoft.EntityFrameworkCore;
 using Microsoft.Extensions.DependencyInjection;
 using Microsoft.Extensions.Hosting;
+using VoiceOfIslam.Api.Data;
+using VoiceOfIslam.Api.Services;
 using VoiceOfIslam.Shared.Models;
 
 var builder = WebApplication.CreateBuilder(args);
 
-// Add services to the container if needed
-// builder.Services.AddCors();
+var connectionString = builder.Configuration.GetConnectionString("DefaultConnection");
+
+builder.Services.AddDbContextFactory<AppDbContext>(options =>
+    options.UseNpgsql(connectionString));
+
+builder.Services.AddScoped<BlobSasService>();
+builder.Services.AddScoped<AudioService>();
+
+builder.Services.AddOptions<BlobStorageOptions>()
+    .BindConfiguration(BlobStorageOptions.SectionName);
 
 var app = builder.Build();
 
@@ -14,49 +25,17 @@ var app = builder.Build();
 app.UseDefaultFiles();
 app.UseStaticFiles();
 
-app.UseRouting(); 
-// API endpoints
-
-// In-memory sample data for demonstration
-var audioStreams = new List<AudioStream>
-{
-    new AudioStream {
-        Id = Guid.NewGuid(),
-        Title = "Sample Lecture 1",
-        Description = "Introduction to Islam",
-        BlobUrl = "https://example.com/audio1.mp3",
-        CreatedAt = DateTime.UtcNow.AddDays(-2),
-        Speaker = "Imam Ali",
-        Duration = TimeSpan.FromMinutes(45)
-    },
-    new AudioStream {
-        Id = Guid.NewGuid(),
-        Title = "Sample Lecture 2",
-        Description = "History of Prophets",
-        BlobUrl = "https://example.com/audio2.mp3",
-        CreatedAt = DateTime.UtcNow.AddDays(-1),
-        Speaker = "Imam Bilal",
-        Duration = TimeSpan.FromMinutes(50)
-    }
-};
-
-// Endpoint: List all audio streams (blobs)
-app.MapGet("/api/blobs", () => audioStreams);
-
-// Endpoint: Download a single audio stream (blob) by ID
-app.MapGet("/api/blobs/{id}", (Guid id) =>
-{
-    var stream = audioStreams.FirstOrDefault(a => a.Id == id);
-    return stream is not null ? Results.Ok(stream) : Results.NotFound();
-});
+app.UseRouting();
 
-// Endpoint: Upload a new audio stream (blob) (optional, demo only)
-app.MapPost("/api/blobs", (AudioStream newStream) =>
+app.MapGet("/api/audio-streams", async (AudioService audioService) => await audioService.GetPastAudios());
+app.MapGet("/api/audio-streams/live", async (AudioService audioService) => await audioService.GetLiveLecture());
+app.MapGet("/api/audio-streams/recent/{count:int}", async (int count, AudioService audioService) => await audioService.GetRecentLectures(count));
+app.MapGet("/api/audio-streams/{id:guid}/playback-url", async (Guid id, AudioService audioService) =>
 {
-    newStream.Id = Guid.NewGuid();
-    newStream.CreatedAt = DateTime.UtcNow;
-    audioStreams.Add(newStream);
-    return Results.Created($"/api/blobs/{newStream.Id}", newStream);
+    var playbackUrl = await audioService.GetAuthorizedPlaybackUrl(id);
+    return string.IsNullOrWhiteSpace(playbackUrl)
+        ? Results.NotFound()
+        : Results.Ok(new PlaybackUrlResponse { Url = playbackUrl });
 });
 
 app.MapFallbackToFile("index.html");

```

---

## Validation

> _Validation LLM call failed: ChatQuotaExceeded: You've exhausted your premium model quota. Please enable additional paid premium requests, upgrade to Copilot Pro+, or wait for your allowance to renew._

---

*Generated by [CodeNarrator](https://github.com/codenarrator/codebase-narrator) · 2026-04-29 12:24:32 UTC*

<!-- bug-entry:diagnostic:IslamApi_Programcs_L20_the-name-blobstorageoptions-does_mok11s40 -->
    ## Bug Report: `IslamApi_Programcs_L20_the-name-blobstorageoptions-does_mok11s40`

| Field | Value |
|---|---|
| **File** | `VoiceOfIslam.Api/Program.cs` |
| **Line** | 20 (col 24) |
| **Source** | unknown |
| **Severity** | 🔴 Error |
| **Detected** | 2026-04-29 12:24:32 UTC |
| **Resolved** | 2026-04-29 12:25:59 UTC |
| **Time to Fix** | 1m 27s |
| **Status** | ✅ Resolved |

---

## Error Message

```
The name 'BlobStorageOptions' does not exist in the current context
```

---

## Code Context

```
  17: builder.Services.AddScoped<AudioService>();
  18: 
  19: builder.Services.AddOptions<BlobStorageOptions>()
  20:     .BindConfiguration(BlobStorageOptions.SectionName); ◄
  21: 
  22: var app = builder.Build();
  23: 
```

---

## Predicted Fix *(CodeNarrator — before any fix was applied)*

> _LLM analysis failed: ChatQuotaExceeded: You've exhausted your premium model quota. Please enable additional paid premium requests, upgrade to Copilot Pro+, or wait for your allowance to renew._

---

## Actual Fix Applied

```diff
diff --git a/VoiceOfIslam.Api/Program.cs b/VoiceOfIslam.Api/Program.cs
index 859c89b..662acb3 100644
--- a/VoiceOfIslam.Api/Program.cs
+++ b/VoiceOfIslam.Api/Program.cs
@@ -1,12 +1,23 @@
 using Microsoft.AspNetCore.Builder;
+using Microsoft.EntityFrameworkCore;
 using Microsoft.Extensions.DependencyInjection;
 using Microsoft.Extensions.Hosting;
+using VoiceOfIslam.Api.Data;
+using VoiceOfIslam.Api.Services;
 using VoiceOfIslam.Shared.Models;
 
 var builder = WebApplication.CreateBuilder(args);
 
-// Add services to the container if needed
-// builder.Services.AddCors();
+var connectionString = builder.Configuration.GetConnectionString("DefaultConnection");
+
+builder.Services.AddDbContextFactory<AppDbContext>(options =>
+    options.UseNpgsql(connectionString));
+
+builder.Services.AddScoped<BlobSasService>();
+builder.Services.AddScoped<AudioService>();
+
+builder.Services.AddOptions<BlobStorageOptions>()
+    .BindConfiguration(BlobStorageOptions.SectionName);
 
 var app = builder.Build();
 
@@ -14,49 +25,17 @@ var app = builder.Build();
 app.UseDefaultFiles();
 app.UseStaticFiles();
 
-app.UseRouting(); 
-// API endpoints
-
-// In-memory sample data for demonstration
-var audioStreams = new List<AudioStream>
-{
-    new AudioStream {
-        Id = Guid.NewGuid(),
-        Title = "Sample Lecture 1",
-        Description = "Introduction to Islam",
-        BlobUrl = "https://example.com/audio1.mp3",
-        CreatedAt = DateTime.UtcNow.AddDays(-2),
-        Speaker = "Imam Ali",
-        Duration = TimeSpan.FromMinutes(45)
-    },
-    new AudioStream {
-        Id = Guid.NewGuid(),
-        Title = "Sample Lecture 2",
-        Description = "History of Prophets",
-        BlobUrl = "https://example.com/audio2.mp3",
-        CreatedAt = DateTime.UtcNow.AddDays(-1),
-        Speaker = "Imam Bilal",
-        Duration = TimeSpan.FromMinutes(50)
-    }
-};
-
-// Endpoint: List all audio streams (blobs)
-app.MapGet("/api/blobs", () => audioStreams);
-
-// Endpoint: Download a single audio stream (blob) by ID
-app.MapGet("/api/blobs/{id}", (Guid id) =>
-{
-    var stream = audioStreams.FirstOrDefault(a => a.Id == id);
-    return stream is not null ? Results.Ok(stream) : Results.NotFound();
-});
+app.UseRouting();
 
-// Endpoint: Upload a new audio stream (blob) (optional, demo only)
-app.MapPost("/api/blobs", (AudioStream newStream) =>
+app.MapGet("/api/audio-streams", async (AudioService audioService) => await audioService.GetPastAudios());
+app.MapGet("/api/audio-streams/live", async (AudioService audioService) => await audioService.GetLiveLecture());
+app.MapGet("/api/audio-streams/recent/{count:int}", async (int count, AudioService audioService) => await audioService.GetRecentLectures(count));
+app.MapGet("/api/audio-streams/{id:guid}/playback-url", async (Guid id, AudioService audioService) =>
 {
-    newStream.Id = Guid.NewGuid();
-    newStream.CreatedAt = DateTime.UtcNow;
-    audioStreams.Add(newStream);
-    return Results.Created($"/api/blobs/{newStream.Id}", newStream);
+    var playbackUrl = await audioService.GetAuthorizedPlaybackUrl(id);
+    return string.IsNullOrWhiteSpace(playbackUrl)
+        ? Results.NotFound()
+        : Results.Ok(new PlaybackUrlResponse { Url = playbackUrl });
 });
 
 app.MapFallbackToFile("index.html");

```

---

## Validation

> _Validation LLM call failed: ChatQuotaExceeded: You've exhausted your premium model quota. Please enable additional paid premium requests, upgrade to Copilot Pro+, or wait for your allowance to renew._

---

*Generated by [CodeNarrator](https://github.com/codenarrator/codebase-narrator) · 2026-04-29 12:24:32 UTC*

<!-- bug-entry:diagnostic:IslamApi_Programcs_L2_the-type-or-namespace_mok11s3z -->
    ## Bug Report: `IslamApi_Programcs_L2_the-type-or-namespace_mok11s3z`

| Field | Value |
|---|---|
| **File** | `VoiceOfIslam.Api/Program.cs` |
| **Line** | 2 (col 17) |
| **Source** | unknown |
| **Severity** | 🔴 Error |
| **Detected** | 2026-04-29 12:24:32 UTC |
| **Resolved** | 2026-04-29 12:28:18 UTC |
| **Time to Fix** | 3m 46s |
| **Status** | ✅ Resolved |

---

## Error Message

```
The type or namespace name 'EntityFrameworkCore' does not exist in the namespace 'Microsoft' (are you missing an assembly reference?)
```

---

## Code Context

```
   1: using Microsoft.AspNetCore.Builder;
   2: using Microsoft.EntityFrameworkCore; ◄
   3: using Microsoft.Extensions.DependencyInjection;
   4: using Microsoft.Extensions.Hosting;
   5: using VoiceOfIslam.Api.Data;
```

---

## Predicted Fix *(CodeNarrator — before any fix was applied)*

> _LLM analysis failed: ChatQuotaExceeded: You've exhausted your premium model quota. Please enable additional paid premium requests, upgrade to Copilot Pro+, or wait for your allowance to renew._

---

## Actual Fix Applied

```diff
diff --git a/VoiceOfIslam.Api/Program.cs b/VoiceOfIslam.Api/Program.cs
index 859c89b..662acb3 100644
--- a/VoiceOfIslam.Api/Program.cs
+++ b/VoiceOfIslam.Api/Program.cs
@@ -1,12 +1,23 @@
 using Microsoft.AspNetCore.Builder;
+using Microsoft.EntityFrameworkCore;
 using Microsoft.Extensions.DependencyInjection;
 using Microsoft.Extensions.Hosting;
+using VoiceOfIslam.Api.Data;
+using VoiceOfIslam.Api.Services;
 using VoiceOfIslam.Shared.Models;
 
 var builder = WebApplication.CreateBuilder(args);
 
-// Add services to the container if needed
-// builder.Services.AddCors();
+var connectionString = builder.Configuration.GetConnectionString("DefaultConnection");
+
+builder.Services.AddDbContextFactory<AppDbContext>(options =>
+    options.UseNpgsql(connectionString));
+
+builder.Services.AddScoped<BlobSasService>();
+builder.Services.AddScoped<AudioService>();
+
+builder.Services.AddOptions<BlobStorageOptions>()
+    .BindConfiguration(BlobStorageOptions.SectionName);
 
 var app = builder.Build();
 
@@ -14,49 +25,17 @@ var app = builder.Build();
 app.UseDefaultFiles();
 app.UseStaticFiles();
 
-app.UseRouting(); 
-// API endpoints
-
-// In-memory sample data for demonstration
-var audioStreams = new List<AudioStream>
-{
-    new AudioStream {
-        Id = Guid.NewGuid(),
-        Title = "Sample Lecture 1",
-        Description = "Introduction to Islam",
-        BlobUrl = "https://example.com/audio1.mp3",
-        CreatedAt = DateTime.UtcNow.AddDays(-2),
-        Speaker = "Imam Ali",
-        Duration = TimeSpan.FromMinutes(45)
-    },
-    new AudioStream {
-        Id = Guid.NewGuid(),
-        Title = "Sample Lecture 2",
-        Description = "History of Prophets",
-        BlobUrl = "https://example.com/audio2.mp3",
-        CreatedAt = DateTime.UtcNow.AddDays(-1),
-        Speaker = "Imam Bilal",
-        Duration = TimeSpan.FromMinutes(50)
-    }
-};
-
-// Endpoint: List all audio streams (blobs)
-app.MapGet("/api/blobs", () => audioStreams);
-
-// Endpoint: Download a single audio stream (blob) by ID
-app.MapGet("/api/blobs/{id}", (Guid id) =>
-{
-    var stream = audioStreams.FirstOrDefault(a => a.Id == id);
-    return stream is not null ? Results.Ok(stream) : Results.NotFound();
-});
+app.UseRouting();
 
-// Endpoint: Upload a new audio stream (blob) (optional, demo only)
-app.MapPost("/api/blobs", (AudioStream newStream) =>
+app.MapGet("/api/audio-streams", async (AudioService audioService) => await audioService.GetPastAudios());
+app.MapGet("/api/audio-streams/live", async (AudioService audioService) => await audioService.GetLiveLecture());
+app.MapGet("/api/audio-streams/recent/{count:int}", async (int count, AudioService audioService) => await audioService.GetRecentLectures(count));
+app.MapGet("/api/audio-streams/{id:guid}/playback-url", async (Guid id, AudioService audioService) =>
 {
-    newStream.Id = Guid.NewGuid();
-    newStream.CreatedAt = DateTime.UtcNow;
-    audioStreams.Add(newStream);
-    return Results.Created($"/api/blobs/{newStream.Id}", newStream);
+    var playbackUrl = await audioService.GetAuthorizedPlaybackUrl(id);
+    return string.IsNullOrWhiteSpace(playbackUrl)
+        ? Results.NotFound()
+        : Results.Ok(new PlaybackUrlResponse { Url = playbackUrl });
 });
 
 app.MapFallbackToFile("index.html");
diff --git a/VoiceOfIslam.Api/VoiceOfIslam.Api.csproj b/VoiceOfIslam.Api/VoiceOfIslam.Api.csproj
index 932f528..3c51b38 100644
--- a/VoiceOfIslam.Api/VoiceOfIslam.Api.csproj
+++ b/VoiceOfIslam.Api/VoiceOfIslam.Api.csproj
@@ -7,7 +7,8 @@
   </PropertyGroup>
 
     <ItemGroup>
-      <ProjectReference Include="..\VoiceOfIslam.Shared\VoiceOfIslam.Shared.csproj" />
+      <PackageReference Include="Azure.Storage.Blobs" Version="12.24.0" />
+      <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="10.0.1" />
     </ItemGroup>
 
      <ItemGroup>

```

---

## Validation

> _Validation LLM call failed: ChatQuotaExceeded: You've exhausted your premium model quota. Please enable additional paid premium requests, upgrade to Copilot Pro+, or wait for your allowance to renew._

---

*Generated by [CodeNarrator](https://github.com/codenarrator/codebase-narrator) · 2026-04-29 12:24:32 UTC*

<!-- bug-entry:diagnostic:IslamApi_Programcs_L7_the-type-or-namespace_mok1420x -->
    ## Bug Report: `IslamApi_Programcs_L7_the-type-or-namespace_mok1420x`

| Field | Value |
|---|---|
| **File** | `VoiceOfIslam.Api/Program.cs` |
| **Line** | 7 (col 20) |
| **Source** | unknown |
| **Severity** | 🔴 Error |
| **Detected** | 2026-04-29 12:26:18 UTC |
| **Resolved** | 2026-04-29 12:28:26 UTC |
| **Time to Fix** | 2m 8s |
| **Status** | ✅ Resolved |

---

## Error Message

```
The type or namespace name 'Shared' does not exist in the namespace 'VoiceOfIslam' (are you missing an assembly reference?)
```

---

## Code Context

```
   4: using Microsoft.Extensions.Hosting;
   5: using VoiceOfIslam.Api.Data;
   6: using VoiceOfIslam.Api.Services;
   7: using VoiceOfIslam.Shared.Models; ◄
   8: 
   9: var builder = WebApplication.CreateBuilder(args);
  10: 
```

---

## Predicted Fix *(CodeNarrator — before any fix was applied)*

> _LLM analysis failed: ChatQuotaExceeded: You've exhausted your premium model quota. Please enable additional paid premium requests, upgrade to Copilot Pro+, or wait for your allowance to renew._

---

## Actual Fix Applied

```diff
diff --git a/VoiceOfIslam.Api/Program.cs b/VoiceOfIslam.Api/Program.cs
index 859c89b..662acb3 100644
--- a/VoiceOfIslam.Api/Program.cs
+++ b/VoiceOfIslam.Api/Program.cs
@@ -1,12 +1,23 @@
 using Microsoft.AspNetCore.Builder;
+using Microsoft.EntityFrameworkCore;
 using Microsoft.Extensions.DependencyInjection;
 using Microsoft.Extensions.Hosting;
+using VoiceOfIslam.Api.Data;
+using VoiceOfIslam.Api.Services;
 using VoiceOfIslam.Shared.Models;
 
 var builder = WebApplication.CreateBuilder(args);
 
-// Add services to the container if needed
-// builder.Services.AddCors();
+var connectionString = builder.Configuration.GetConnectionString("DefaultConnection");
+
+builder.Services.AddDbContextFactory<AppDbContext>(options =>
+    options.UseNpgsql(connectionString));
+
+builder.Services.AddScoped<BlobSasService>();
+builder.Services.AddScoped<AudioService>();
+
+builder.Services.AddOptions<BlobStorageOptions>()
+    .BindConfiguration(BlobStorageOptions.SectionName);
 
 var app = builder.Build();
 
@@ -14,49 +25,17 @@ var app = builder.Build();
 app.UseDefaultFiles();
 app.UseStaticFiles();
 
-app.UseRouting(); 
-// API endpoints
-
-// In-memory sample data for demonstration
-var audioStreams = new List<AudioStream>
-{
-    new AudioStream {
-        Id = Guid.NewGuid(),
-        Title = "Sample Lecture 1",
-        Description = "Introduction to Islam",
-        BlobUrl = "https://example.com/audio1.mp3",
-        CreatedAt = DateTime.UtcNow.AddDays(-2),
-        Speaker = "Imam Ali",
-        Duration = TimeSpan.FromMinutes(45)
-    },
-    new AudioStream {
-        Id = Guid.NewGuid(),
-        Title = "Sample Lecture 2",
-        Description = "History of Prophets",
-        BlobUrl = "https://example.com/audio2.mp3",
-        CreatedAt = DateTime.UtcNow.AddDays(-1),
-        Speaker = "Imam Bilal",
-        Duration = TimeSpan.FromMinutes(50)
-    }
-};
-
-// Endpoint: List all audio streams (blobs)
-app.MapGet("/api/blobs", () => audioStreams);
-
-// Endpoint: Download a single audio stream (blob) by ID
-app.MapGet("/api/blobs/{id}", (Guid id) =>
-{
-    var stream = audioStreams.FirstOrDefault(a => a.Id == id);
-    return stream is not null ? Results.Ok(stream) : Results.NotFound();
-});
+app.UseRouting();
 
-// Endpoint: Upload a new audio stream (blob) (optional, demo only)
-app.MapPost("/api/blobs", (AudioStream newStream) =>
+app.MapGet("/api/audio-streams", async (AudioService audioService) => await audioService.GetPastAudios());
+app.MapGet("/api/audio-streams/live", async (AudioService audioService) => await audioService.GetLiveLecture());
+app.MapGet("/api/audio-streams/recent/{count:int}", async (int count, AudioService audioService) => await audioService.GetRecentLectures(count));
+app.MapGet("/api/audio-streams/{id:guid}/playback-url", async (Guid id, AudioService audioService) =>
 {
-    newStream.Id = Guid.NewGuid();
-    newStream.CreatedAt = DateTime.UtcNow;
-    audioStreams.Add(newStream);
-    return Results.Created($"/api/blobs/{newStream.Id}", newStream);
+    var playbackUrl = await audioService.GetAuthorizedPlaybackUrl(id);
+    return string.IsNullOrWhiteSpace(playbackUrl)
+        ? Results.NotFound()
+        : Results.Ok(new PlaybackUrlResponse { Url = playbackUrl });
 });
 
 app.MapFallbackToFile("index.html");
diff --git a/VoiceOfIslam.Api/VoiceOfIslam.Api.csproj b/VoiceOfIslam.Api/VoiceOfIslam.Api.csproj
index 932f528..3c51b38 100644
--- a/VoiceOfIslam.Api/VoiceOfIslam.Api.csproj
+++ b/VoiceOfIslam.Api/VoiceOfIslam.Api.csproj
@@ -7,7 +7,8 @@
   </PropertyGroup>
 
     <ItemGroup>
-      <ProjectReference Include="..\VoiceOfIslam.Shared\VoiceOfIslam.Shared.csproj" />
+      <PackageReference Include="Azure.Storage.Blobs" Version="12.24.0" />
+      <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="10.0.1" />
     </ItemGroup>
 
      <ItemGroup>

```

---

## Validation

> _Validation LLM call failed: ChatQuotaExceeded: You've exhausted your premium model quota. Please enable additional paid premium requests, upgrade to Copilot Pro+, or wait for your allowance to renew._

---

*Generated by [CodeNarrator](https://github.com/codenarrator/codebase-narrator) · 2026-04-29 12:26:18 UTC*

<!-- bug-entry:diagnostic:IslamApi_Programcs_L38_the-type-or-namespace_mok17h8n -->
    ## Bug Report: `IslamApi_Programcs_L38_the-type-or-namespace_mok17h8n`

| Field | Value |
|---|---|
| **File** | `VoiceOfIslam.Api/Program.cs` |
| **Line** | 38 (col 26) |
| **Source** | unknown |
| **Severity** | 🔴 Error |
| **Detected** | 2026-04-29 12:28:58 UTC |
| **Resolved** | 2026-04-29 12:29:39 UTC |
| **Time to Fix** | 41s |
| **Status** | ✅ Resolved |

---

## Error Message

```
The type or namespace name 'PlaybackUrlResponse' could not be found (are you missing a using directive or an assembly reference?)
```

---

## Code Context

```
  35:     var playbackUrl = await audioService.GetAuthorizedPlaybackUrl(id);
  36:     return string.IsNullOrWhiteSpace(playbackUrl)
  37:         ? Results.NotFound()
  38:         : Results.Ok(new PlaybackUrlResponse { Url = playbackUrl }); ◄
  39: });
  40: 
  41: app.MapFallbackToFile("index.html");
```

---

## Predicted Fix *(CodeNarrator — before any fix was applied)*

> _LLM analysis failed: ChatQuotaExceeded: You've exhausted your premium model quota. Please enable additional paid premium requests, upgrade to Copilot Pro+, or wait for your allowance to renew._

---

## Actual Fix Applied

```diff
diff --git a/VoiceOfIslam.Api/Program.cs b/VoiceOfIslam.Api/Program.cs
index 859c89b..662acb3 100644
--- a/VoiceOfIslam.Api/Program.cs
+++ b/VoiceOfIslam.Api/Program.cs
@@ -1,12 +1,23 @@
 using Microsoft.AspNetCore.Builder;
+using Microsoft.EntityFrameworkCore;
 using Microsoft.Extensions.DependencyInjection;
 using Microsoft.Extensions.Hosting;
+using VoiceOfIslam.Api.Data;
+using VoiceOfIslam.Api.Services;
 using VoiceOfIslam.Shared.Models;
 
 var builder = WebApplication.CreateBuilder(args);
 
-// Add services to the container if needed
-// builder.Services.AddCors();
+var connectionString = builder.Configuration.GetConnectionString("DefaultConnection");
+
+builder.Services.AddDbContextFactory<AppDbContext>(options =>
+    options.UseNpgsql(connectionString));
+
+builder.Services.AddScoped<BlobSasService>();
+builder.Services.AddScoped<AudioService>();
+
+builder.Services.AddOptions<BlobStorageOptions>()
+    .BindConfiguration(BlobStorageOptions.SectionName);
 
 var app = builder.Build();
 
@@ -14,49 +25,17 @@ var app = builder.Build();
 app.UseDefaultFiles();
 app.UseStaticFiles();
 
-app.UseRouting(); 
-// API endpoints
-
-// In-memory sample data for demonstration
-var audioStreams = new List<AudioStream>
-{
-    new AudioStream {
-        Id = Guid.NewGuid(),
-        Title = "Sample Lecture 1",
-        Description = "Introduction to Islam",
-        BlobUrl = "https://example.com/audio1.mp3",
-        CreatedAt = DateTime.UtcNow.AddDays(-2),
-        Speaker = "Imam Ali",
-        Duration = TimeSpan.FromMinutes(45)
-    },
-    new AudioStream {
-        Id = Guid.NewGuid(),
-        Title = "Sample Lecture 2",
-        Description = "History of Prophets",
-        BlobUrl = "https://example.com/audio2.mp3",
-        CreatedAt = DateTime.UtcNow.AddDays(-1),
-        Speaker = "Imam Bilal",
-        Duration = TimeSpan.FromMinutes(50)
-    }
-};
-
-// Endpoint: List all audio streams (blobs)
-app.MapGet("/api/blobs", () => audioStreams);
-
-// Endpoint: Download a single audio stream (blob) by ID
-app.MapGet("/api/blobs/{id}", (Guid id) =>
-{
-    var stream = audioStreams.FirstOrDefault(a => a.Id == id);
-    return stream is not null ? Results.Ok(stream) : Results.NotFound();
-});
+app.UseRouting();
 
-// Endpoint: Upload a new audio stream (blob) (optional, demo only)
-app.MapPost("/api/blobs", (AudioStream newStream) =>
+app.MapGet("/api/audio-streams", async (AudioService audioService) => await audioService.GetPastAudios());
+app.MapGet("/api/audio-streams/live", async (AudioService audioService) => await audioService.GetLiveLecture());
+app.MapGet("/api/audio-streams/recent/{count:int}", async (int count, AudioService audioService) => await audioService.GetRecentLectures(count));
+app.MapGet("/api/audio-streams/{id:guid}/playback-url", async (Guid id, AudioService audioService) =>
 {
-    newStream.Id = Guid.NewGuid();
-    newStream.CreatedAt = DateTime.UtcNow;
-    audioStreams.Add(newStream);
-    return Results.Created($"/api/blobs/{newStream.Id}", newStream);
+    var playbackUrl = await audioService.GetAuthorizedPlaybackUrl(id);
+    return string.IsNullOrWhiteSpace(playbackUrl)
+        ? Results.NotFound()
+        : Results.Ok(new PlaybackUrlResponse { Url = playbackUrl });
 });
 
 app.MapFallbackToFile("index.html");
diff --git a/VoiceOfIslam.Api/VoiceOfIslam.Api.csproj b/VoiceOfIslam.Api/VoiceOfIslam.Api.csproj
index 932f528..a859a5d 100644
--- a/VoiceOfIslam.Api/VoiceOfIslam.Api.csproj
+++ b/VoiceOfIslam.Api/VoiceOfIslam.Api.csproj
@@ -7,10 +7,9 @@
   </PropertyGroup>
 
     <ItemGroup>
+      <PackageReference Include="Azure.Storage.Blobs" Version="12.24.0" />
+      <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="10.0.1" />
       <ProjectReference Include="..\VoiceOfIslam.Shared\VoiceOfIslam.Shared.csproj" />
-    </ItemGroup>
-
-     <ItemGroup>
       <Content Include="wwwroot\**\*" CopyToPublishDirectory="PreserveNewest" />
     </ItemGroup>
 

```

---

## Validation

> _Validation LLM call failed: ChatQuotaExceeded: You've exhausted your premium model quota. Please enable additional paid premium requests, upgrade to Copilot Pro+, or wait for your allowance to renew.
> 
> Note: GitHub is currently experiencing a service disruption. This may be affecting Copilot. Check [GitHub Status](https://www.githubstatus.com) for details._

---

*Generated by [CodeNarrator](https://github.com/codenarrator/codebase-narrator) · 2026-04-29 12:28:58 UTC*
